namespace Ozy.HomeSeerDimmers.Apps.Dimmers
{
    /// <summary>
    /// Data used when Home Assistant calls to synchronize dimmer states
    /// </summary>
    public record class SynchronizeDimmersData();
}
